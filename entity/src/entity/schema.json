{
  "$schema": "http://json-schema.org/schema",
  "$id": "Entity generate",
  "title": "ng new options schema",
  "type": "object",
  "description": "Initialise a new entity",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the entity",
      "x-prompt": "Name:",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  }
}
